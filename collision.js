// class MouvObjet {
//   constructor(x, y, size) {
//     this.x = x;
//     this.y = y;
//     this.size = size;
//   }
//   test (other) {
//     if(Math.abs(this.x - other.x) < (this.size + other.size) / 2 &&
//       Math.abs(this.y - other.y) < (this.size + other.size) / 2)
//       return true
//     return false
//   }
// }
